{
  "name": "sjs-frontend",
  "version": "1.0.0",
  "description": "SJS Digital Solutions - Frontend",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html css js dist/",
    "start": "npx serve ."
  },
  "engines": {
    "node": ">=18"
  }
}
